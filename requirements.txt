# Kilo Code — Python automation & scheduler layer
# Managed by Kilo Code.
#
# fastapi>=0.103  — monitor/api.py requires this; 0.95.x is incompatible
# with pydantic 2.13+ (Undefined removed from pydantic.fields).
# Pin: fastapi==0.115.6 in production.

# ADB interaction
pure-python-adb>=0.3.0.dev0

# Device automation (screen touch/swipe)
scrcpy-client>=0.1.0

# Scheduling / timing
schedule>=1.2.0
apscheduler>=3.11.0

# Configuration management
pyyaml>=6.0
pydantic>=2.0.0

# Logging
loguru>=0.7.0

# HTTP / status API  — fastapi≥0.103 is a hard compat floor for pydantic 2.13+
fastapi==0.115.6
uvicorn[standard]>=0.23.0
requests>=2.31.0

# AI/ML Dependencies
numpy>=1.24.0
pillow>=9.0.0
scikit-learn>=1.3.0
opencv-python>=4.7.0

# Utilities
python-dotenv>=1.0.0
psutil>=5.9.0

# PyInstaller — bundle CLI into standalone executable
pyinstaller>=6.0
